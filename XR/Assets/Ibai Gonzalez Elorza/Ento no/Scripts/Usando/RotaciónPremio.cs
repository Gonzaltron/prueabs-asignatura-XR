using UnityEngine;

public class RotaciónPremio : MonoBehaviour
{
    /*
    Vector3 initialPos;
    Vector3 endPos = new Vector3(10, 0, 0);
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        initialPos = this.transform.rotation;
        this.transform.position = Vector3.Lerp(initialPos, endPos, 3f);
        endPos = new Vector3(endPos.x++, endPos.y++, endPos.z++);
    }
    */
}
